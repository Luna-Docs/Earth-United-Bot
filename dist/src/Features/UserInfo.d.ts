import { GuildMember, Message } from "discord.js";
import Cluster from "../Lib/Structures/Client";
declare const _default: (message: Message, client: Cluster, member: GuildMember, args: string[]) => Promise<Message | undefined>;
export = _default;
